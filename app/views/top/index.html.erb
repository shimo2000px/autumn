<div class="autumn-top relative min-h-screen flex flex-col items-center justify-center">

  <!-- 背景 -->
  <img src="<%= asset_path('top.png') %>"
      alt="ゲーム背景"
      class="absolute inset-0 w-full h-full object-cover z-0">

  <!-- タイトル -->
  <h1 class="
      absolute z-10 font-bold text-amber-600
      text-4xl sm:text-5xl md:text-4xl lg:text-6xl
      top-48 sm:top-64 md:top-80
      border-b-8 border-yellow-600 pb-2">
    ひつじさんの秋コーデ工房
  </h1>

  <!-- ボタン -->
  <div class="
      absolute z-10 flex space-x-4 sm:space-x-6
      bottom-20 sm:bottom-28 md:bottom-30
    ">
    <%= link_to "遊び方", usage_path,
      class: "px-6 py-3 sm:px-8 sm:py-4 md:px-12 md:py-6 bg-red-500 hover:bg-red-400 rounded-xl text-lg sm:text-xl md:text-2xl text-white font-semibold transition" %>

    <%= link_to "ゲームを始める", games_path,
      class: "px-6 py-3 sm:px-8 sm:py-4 md:px-12 md:py-6 bg-yellow-500 hover:bg-yellow-400 text-white rounded-xl text-lg sm:text-xl md:text-2xl font-bold transition" %>
  </div>
</div>
